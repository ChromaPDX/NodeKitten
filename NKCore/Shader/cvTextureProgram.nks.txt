@frag

//    float texX = (v_texCoord0.x) * 640.;
//    float texY = (v_texCoord0.y) * 360.;
//    vec2 texc = vec2(texX, texY);

    texColor = texture(u_textureRect,v_texCoord0*u_textureScale);
    if (texColor.a < .1) discard;

@frag