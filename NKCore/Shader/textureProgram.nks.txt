@frag

    texColor =  texture(u_texture,v_texCoord0.xy); //
   // if (texColor.a < .05) discard;

@frag

@fragES

    texColor =  texture2D(u_texture,v_texCoord0.xy); //
    //if (texColor.a < .05) discard;

@fragES